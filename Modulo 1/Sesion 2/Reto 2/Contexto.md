### Reto 2: Clasificador de Números 🔁
-   **Objetivo:** Practicar el uso de múltiples condiciones (`if`, `else if`, `else`) y operadores lógicos.
-   **Descripción:** En este reto, implementamos un programa que clasificara un número como positivo, negativo o cero. Adicionalmente, si el número era positivo, determinaba si era par o impar. Esto nos permitió reforzar el anidamiento de condicionales y el uso de operadores como el módulo (`%`) para tomar decisiones más complejas.
