### Reto 1: Objeto Coche 🔑
-   **Objetivo:** Crear una clase y objetos, definir atributos y métodos simples.
-   **Descripción:** Este reto nos desafió a diseñar una clase `Coche` con atributos como marca, modelo, año y color, y un constructor para inicializarlos. Además, implementamos métodos como `mostrarInformacion()` y `encender()`. Luego, en el programa principal, creamos múltiples objetos `Coche` y utilizamos sus métodos para ver cómo cada instancia mantenía su propio estado y comportamiento.
